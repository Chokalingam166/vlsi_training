
module full_subtractor (
    input A,      
    input B,      
    input Bin,   
    output Diff, 
    output Bout  
);
    // Difference = A XOR B XOR Bin
    assign Diff = A ^ B ^ Bin;
    // Borrow = (~A & B) | (~(A ^ B) & Bin)
    assign Bout = (~A & B) | (~(A ^ B) & Bin);
endmodule
